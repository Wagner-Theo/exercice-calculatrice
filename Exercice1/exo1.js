function calculatrice(nb1, sign, nb2)
{
    if (sign === "+")
    return(nb1 + nb2)
    if (sign === "-")
    return(nb1 - nb2)
    if (sign === "*")
    return(nb1 * nb2)
    if (sign === "/")
    return(nb1 / nb2)
}